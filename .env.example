# Copy this file to .env locally or export the variable in your shell.
# Never commit a real API key.
ARK_API_KEY=ark-REPLACE_WITH_YOUR_KEY

# Optional when ffmpeg is not available in PATH.
# FFMPEG=/opt/homebrew/bin/ffmpeg
# FFPROBE=/opt/homebrew/bin/ffprobe
